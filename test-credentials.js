// Test Login Credentials for Story App
// You can use these credentials to test the login functionality

const TEST_CREDENTIALS = {
  email: 'testuser@dicoding.com',
  password: 'testpassword123',
};

console.log('Test credentials created. Use the following to test login:');
console.log('Email:', TEST_CREDENTIALS.email);
console.log('Password:', TEST_CREDENTIALS.password);

// You can also register a new user with these credentials if needed
const REGISTRATION_DATA = {
  name: 'Test User',
  email: 'testuser2@dicoding.com',
  password: 'testpassword123',
};

console.log('\nFor registration test:');
console.log('Name:', REGISTRATION_DATA.name);
console.log('Email:', REGISTRATION_DATA.email);
console.log('Password:', REGISTRATION_DATA.password);
